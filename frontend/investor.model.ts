export interface Investor {
    id: number;                 // Unique identifier for the investor
    name: string;               // Name of the investor
    totalCommitments: number;   // Total amount of commitments by the investor
  }